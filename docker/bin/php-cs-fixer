#!/usr/bin/env bash

set -e

BASEDIR=$(dirname "$0")
FULLDIR=$PWD${BASEDIR#.}

docker run --rm --interactive --tty \
    --user $(id -u):$(id -g) \
    --volume ${FULLDIR}/../../project:/project \
    herloct/php-cs-fixer fix "$@"
